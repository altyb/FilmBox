# TMDB API Read Access Token (v4 auth) — https://www.themoviedb.org/settings/api
# The app falls back to a committed read-only token if this is unset, so a
# fresh clone runs without setup. Set your own before deploying.
VITE_TMDB_TOKEN=
